name: Build Kernel Picasso Ultimate Official (Final Fix KSU)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@v10
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'

      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential flex libssl-dev libelf-dev clang lld llvm repo

      - name: Sync Source
        run: |
          mkdir kernel-source && cd kernel-source
          repo init -u https://github.com/DVWorkspaces/kernel_gki_android12-9 -b nethunter
          repo sync -c -j$(nproc) --force-sync --no-clone-bundle --no-tags

      - name: Integrate KernelSU (Direct Core Injection)
        run: |
          # Masuk ke folder inti kernel
          cd kernel-source/common
          
          # Jalankan setup resmi
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
          
          # FIX TERBAIK: Daftarkan KernelSU di Makefile inti (Core Makefile)
          # Kita tambahkan ksu ke dalam daftar objek yang wajib di-build
          sed -i '/drivers-y/i obj-y += drivers/kernelsu/' Makefile
          
          # Verifikasi lokasi (Pastikan folder ksu ada di drivers)
          ls drivers/kernelsu/ || echo "Folder KSU tidak ditemukan!"

      - name: Patch Config (Ultimate Features)
        run: |
          cd kernel-source/common
          CONF="arch/arm64/configs/gki_defconfig"
          
          # Clean & Set KSU
          sed -i '/CONFIG_KSU/d' $CONF
          echo "CONFIG_KSU=y" >> $CONF
          
          # SELINUX SAKTI (Develop & Bootparam)
          sed -i '/CONFIG_SECURITY_SELINUX_DEVELOP/d' $CONF
          sed -i '/CONFIG_SECURITY_SELINUX_BOOTPARAM/d' $CONF
          echo "CONFIG_SECURITY_SELINUX_DEVELOP=y" >> $CONF
          echo "CONFIG_SECURITY_SELINUX_BOOTPARAM=y" >> $CONF
          
          # CMDLINE EXTEND (Anti Panic EROFS)
          sed -i '/CONFIG_CMDLINE_FORCE/d' $CONF
          sed -i '/CONFIG_CMDLINE=/d' $CONF
          echo "CONFIG_CMDLINE=\"androidboot.selinux=permissive androidboot.veritymode=logging\"" >> $CONF
          echo "CONFIG_CMDLINE_EXTEND=y" >> $CONF

          # SAFETY LOGS (PSTORE/RAMOOPS)
          echo "CONFIG_PSTORE=y" >> $CONF
          echo "CONFIG_PSTORE_CONSOLE=y" >> $CONF
          echo "CONFIG_PSTORE_PMSG=y" >> $CONF
          echo "CONFIG_PSTORE_RAM=y" >> $CONF
          sed -i '/CONFIG_PANIC_TIMEOUT/d' $CONF
          echo "CONFIG_PANIC_TIMEOUT=30" >> $CONF

          # DISABLE LTO
          sed -i 's/CONFIG_LTO_CLANG_THIN=y/CONFIG_LTO_CLANG_NONE=y/g' $CONF
          sed -i 's/CONFIG_LTO_CLANG_FULL=y/CONFIG_LTO_CLANG_NONE=y/g' $CONF
          echo "CONFIG_LTO_CLANG_NONE=y" >> $CONF

      - name: Build Kernel
        run: |
          cd kernel-source
          export LTO=none
          export BUILD_CONFIG=common/build.config.gki.aarch64
          export SKIP_MRPROPER=1
          build/build.sh

      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-Picasso-Ultimate-Fixed
          path: kernel-source/out/android12-5.10/dist/Image.gz
          
